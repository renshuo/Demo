CoroutineVerticleTest:
  config:
    cfa: "a"
  worker: false # default is false
  multiThreaded: false # default is false

WebVerticle:
  config:
    port: 8082

WebSockJsVerticle:
  config:
    port: 8083 # for http server use
    heartbeatInterval: 25000 # millisecond
    sessionTimeout: 5 # second

AuthTest:
  config:

JdbcWorker:
  workder: true
  config:
    driver_class: org.mariadb.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test
    user: root
    password: "123"

